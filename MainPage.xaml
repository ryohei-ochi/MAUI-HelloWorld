<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HelloWorld.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="TestBtn"
                Text="Run App Icon Tests" 
                SemanticProperties.Hint="Runs verification tests for app icon display"
                Clicked="OnTestClicked"
                HorizontalOptions="Fill"
                BackgroundColor="LightBlue" />

            <Button
                x:Name="SplashTestBtn"
                Text="Run Splash Screen Tests" 
                SemanticProperties.Hint="Runs verification tests for splash screen display"
                Clicked="OnSplashTestClicked"
                HorizontalOptions="Fill"
                BackgroundColor="LightGreen" />

            <Button
                x:Name="PlatformTestBtn"
                Text="Run Platform-Specific Tests" 
                SemanticProperties.Hint="Runs platform-specific verification tests for Android, iOS, and Windows"
                Clicked="OnPlatformTestClicked"
                HorizontalOptions="Fill"
                BackgroundColor="Purple" />

            <Button
                x:Name="IntegrationTestBtn"
                Text="Run Integration Tests" 
                SemanticProperties.Hint="Runs integration tests for overall branding functionality"
                Clicked="OnIntegrationTestClicked"
                HorizontalOptions="Fill"
                BackgroundColor="DarkRed" />

            <Button
                x:Name="AllTestsBtn"
                Text="Run All Branding Tests" 
                SemanticProperties.Hint="Runs all verification tests for app branding"
                Clicked="OnAllTestsClicked"
                HorizontalOptions="Fill"
                BackgroundColor="Orange" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
